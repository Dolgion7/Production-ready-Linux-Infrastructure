# Production-ready-Linux-Infrastructure
Designed and deployed a secure, production-ready Linux server. Configured Ubuntu VPS with SSH key authentication, disabled root access, and hardened security using UFW and Fail2Ban. Deployed a Node.js app via Nginx with HTTPS (Let’s Encrypt) and automated backups and updates using Bash and cron.
